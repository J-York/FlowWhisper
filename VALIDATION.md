# 验证记录

验证日期：2026-09-19。环境：Windows 11 x64、Node.js 22.23.2、Electron 44.4.2。

- `npm run build`：通过 TypeScript 检查、Vite 构建与 Electron 主进程打包。
- `npm test`：6 个测试文件、41 项测试全部通过。覆盖快捷键解析、Gemini 本地 WebSocket、火山协议帧与最终包、WAV 上传、PCM 尾包、配置校验、完整提示词传输与迁移、界面消息翻译、润色回退、取消和断线保留部分文字。
- `npm run test:desktop`：开发版通过；使用 `FLOWWHISPER_EXECUTABLE=release/win-unpacked/FlowWhisper.exe` 的实际打包程序也通过。
- 桌面测试覆盖 6 个页面、真实 AudioWorklet 管线（使用虚拟麦克风）、本地模拟模型接口、转写与润色、历史、取消、密钥加密、剪贴板复制和富文本恢复。
- Windows 测试环境拒绝激活测试输入窗口时，已验证应用阻止自动输入并回退剪贴板。富文本剪贴板恢复通过注入成功的粘贴回调单独验证。跨应用键盘输入还需在目标应用获得前台焦点的交互环境中验证，不把该回退测试视为所有应用的自动粘贴兼容性验证。
- `npm audit`：0 个已知漏洞。
- `npm run dist:win -- --publish never`：生成 Windows x64 NSIS 安装包。安装包未签名。

## 尚未验证

- Groq、火山引擎及其他第三方服务仍未完成真实账户联调；Gemini 已完成下述合成语音收尾测试，不能代替不同语言、噪声环境及长段语音的识别质量验证。
- 当前环境没有 macOS，未完成 macOS 实机构建、麦克风/辅助功能授权、跨应用输入或 DMG 安装验证。仓库包含 arm64/x64 打包配置与 CI 工作流。
- 未做代码签名、公证、安装器实际安装/卸载操作，也未做长时间稳定性和多设备麦克风热插拔测试。

自动化桌面测试使用独立的临时用户目录，不写入正常应用配置；模拟服务与虚拟麦克风仅出现在测试脚本中。真实 Gemini 联调在原应用进程内使用已保存的凭据，发送本地合成语音，不录制麦克风、不修改设置或历史、不输出密钥。截图及诊断记录存放在忽略提交的 `test-results/`。

## 0.1.1 浮层透明修复

将浅色背景从整个 HTML 文档移到主界面容器，避免透明窗口的圆角外露出矩形底色。通过 Electron 原生窗口截图对比验证：修复前四角 RGBA 为 `(247, 248, 250, 255)`，修复后为 `(0, 0, 0, 0)`；提示条仍可见，并保留轻微阴影。验证图片位于 `test-results/overlay-before.png` 和 `test-results/overlay-after.png`。

## 0.1.2 Gemini 收尾修复

真实接口复现了误报超时：停止后约 385 ms 已收到最终转写和 `generationComplete`，但服务没有发送原实现一直等待的 `turnComplete`。现在同时识别两类结束通知，并等待最终转写，兼容结束通知先于文字到达的情况；仅有临时文字时仍保留超时保护，避免把未确认的最后一句当成完整结果。

真实接口回测使用约 5.98 秒的英文合成语音，两次分别在停止后 396 ms、427 ms 完成，输出包含预期的最后一句。全零 PCM 静音不会获得最终转写，已增加本地直接返回空结果的处理；检测不使用音量阈值，非零的微弱声音仍交给服务识别。服务端未识别出语言的其他音频仍可能超时。

新增 7 项回归用例：仅收到 `generationComplete`、两类结束通知分别先于最终文字到达、明确的空转写、临时文字超时、全零静音，以及单个最低有效位的声音信号不被误判为空。真实接口记录仅保留消息类型、耗时和文字长度，位于 `test-results/gemini-events.json` 和 `test-results/gemini-live-verification.json`。

0.1.2 Windows 安装包构建通过，实际打包程序再次通过桌面回归测试（本地模拟识别服务、虚拟麦克风、润色、历史、取消、剪贴板与恢复）。Windows 前台激活限制仍使自动粘贴走受保护的剪贴板回退，未将其记作跨应用粘贴成功。

## 0.1.3 小圆球与快捷键录制

录音浮层调整为 38×38 px 圆球，置于当前屏幕工作区右侧中部；透明窗口为 64×64 px，不显示转写正文，不抢焦点且鼠标穿透。桌面回归检查圆球与窗口尺寸，原生截图角落像素为 `(0, 0, 0, 0)`。不加入实时修改外部输入框的行为，继续在识别及润色结束后一次性输入。

快捷键支持点击录制后按组合键，松开所有按键后完成捕获，再保存生效。验证了录制期间暂停原快捷键、保存新组合、Esc 取消、失焦恢复，且未误触发录音；解析测试包含 macOS Command 与 Windows Control / Super、字母和功能键，以及忽略裸按键、输入法组合过程和重复事件。macOS 按键录制尚未完成实机验证。

界面截图位于 `test-results/hotkey-recorder.png`、`test-results/recording-orb.png`。开发版与 0.1.3 Windows 实际打包程序均通过桌面回归测试，成功在测试输入框执行 Windows 粘贴并验证恢复剪贴板；仍不代表所有第三方应用的兼容性。

## 0.1.4 拖动小圆球

采用指针捕获与窗口定位实现拖动，保留 `focusable: false`，无额外依赖。松开鼠标时将位置独立保存到 `window-state.json`；转写状态刷新不再重置位置。显示器移除、工作区变化或保存的位置不可见时，将小球限制在可见区域。为接收拖动操作，取消整个浮层的鼠标穿透。

`npm run test:drag` 在隔离配置中使用 Windows 实际鼠标按下、移动和松开测试，并校验前台窗口句柄保持不变。覆盖拖动后继续转写、再次开始录音、完整重启后的坐标恢复，以及屏幕外坐标恢复。测试不采集麦克风或调用模型服务，结果存放在 `test-results/drag-verification.json`。macOS 尚未实机验证拖动行为。

31 项单元测试通过。开发版与 0.1.4 Windows 实际打包程序均通过拖动测试，实际打包程序也通过完整桌面回归（包括透明度、快捷键录制和转写流程）。

## 0.1.5 移除面包屑

移除六个页面共用的顶部当前位置提示及对应图标、样式，保留左侧导航高亮。顶部操作区从 67px 缩为 44px，并移除分隔线与单独底色。TypeScript 与生产构建通过，实际 Windows 打包程序通过桌面回归；查看设置页截图确认布局正常。此次原生粘贴测试因前台激活限制走受保护的剪贴板回退。

## 0.1.6 精简界面

六个页面移除宣传式标题、重复说明、侧栏分组文案、示例卡片与装饰性页脚。页面保留简短功能标题，保存操作合并到同一行。首页将模型选择与常用开关集中为一排，录音按钮与状态并列，转写区扩展为主栏全宽，正文增至 18px；仅在有记录时显示最近输入。

配置说明改为默认收起，字段帮助通过鼠标悬停或键盘聚焦问号查看。保留错误、权限、快捷键录制过程与结果提示。精简仅涉及界面，识别、润色和停止后一次性输入的流程保持原有实现。

TypeScript 与生产构建通过。使用隔离配置在 1180×800 与最小 900×680 窗口检查六个页面，无横向溢出；验证字段帮助可通过键盘访问、提示框没有越过窗口边界，折叠说明可展开。查看首页、模型、润色和偏好设置截图确认布局。记录位于 `test-results/layout-verification.json`。

Windows 0.1.6 安装包生成成功，实际打包程序通过完整桌面回归，包括虚拟麦克风转写、润色、快捷键录制、小球尺寸与透明度、历史、取消、剪贴板恢复，并成功向获得焦点的 Windows 测试输入框粘贴。macOS 实机验证仍未完成。

## 0.1.7 滚动条、完整提示词、主题与英文

主窗口使用独立滚动容器及稳定的滚动条占位，展开说明时不再改变内容宽度；规则不作用于透明浮层。回归测试先让润色页完整容纳收起的内容，再展开真实说明触发垂直溢出：内容区宽度前后均为 1165px，提示词框 x 坐标均为 275px、宽度均为 829px。

润色页展示并直接编辑完整系统提示词，提供三种默认模板和恢复默认；替换自定义提示词前显示确认。Gemini 的 `systemInstruction` 和兼容接口的 system 消息仅使用编辑后的内容，除了明确的 `{{dictionary}}` 占位符替换，不附加隐藏指令。旧版 `instruction` 迁移到编辑框中；自定义内容在切换语言、保存、重新解析和完整重启后保持不变。新增测试验证两类实际请求结构、迁移、空白与超长输入校验，以及特殊字符词条的字面替换。

新增简体中文 / English，以及跟随系统 / 浅色 / 暗色。界面语言与识别语言独立；默认模板可随界面语言切换，自定义提示词保持原文。英文覆盖六页控件、帮助、提示、应用错误和托盘菜单；系统设备名称及用户文本保留原语言。使用统一主题变量，并通过 Electron 原生主题事件同步系统变化、主窗口及浮层。保存后同步原生标题栏、托盘菜单，并在重启时恢复设置。

`npm run test:preferences` 使用独立临时配置，验证旧配置升级、编辑及恢复提示词、24 组英文页面/主题/窗口尺寸组合、真实展开造成的滚动条宽度稳定、原生主题变更同步、浮层设置同步和完整重启持久化。所有组合没有横向溢出；检查中英文及明暗主题截图。记录位于 `test-results/preferences-verification.json`，截图以 `preferences-` 为前缀。

41 项单元测试及 TypeScript / 生产构建通过。0.1.7 Windows 安装包生成成功，实际打包程序通过 `test:desktop` 和 `test:preferences`。桌面测试保留原生浮层四角完全透明的检查，并成功向已聚焦的 Windows 测试输入框粘贴及恢复剪贴板。此次未调用真实模型服务，提示词传输通过模拟请求验证；macOS 的主题、语言及实际输入行为仍需实机验证。
